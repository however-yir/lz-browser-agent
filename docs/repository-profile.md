# 仓库品牌信息（Repository Profile）

## 推荐仓库名称
`lz-browser-agent`

## 推荐仓库描述（GitHub Description）
本地优先的浏览器自动化智能体框架，支持 OpenAI 兼容接口、Ollama、本地部署与可扩展工具链。

## 推荐 GitHub Topics
`browser-automation` `llm-agent` `playwright` `python` `ollama` `openai-compatible` `local-first` `ai-tools`

## 设置命令示例（GitHub CLI）
```bash
gh repo edit your-org/lz-browser-agent \
  --description "本地优先的浏览器自动化智能体框架，支持 OpenAI 兼容接口、Ollama、本地部署与可扩展工具链。" \
  --add-topic browser-automation \
  --add-topic llm-agent \
  --add-topic playwright \
  --add-topic python \
  --add-topic ollama \
  --add-topic openai-compatible \
  --add-topic local-first \
  --add-topic ai-tools
```

## Logo 使用建议
- README 顶部横幅：`static/lz-browser-agent-logo.svg`
- 社交分享图（可选）：后续新增 `static/lz-browser-agent-og.png`
